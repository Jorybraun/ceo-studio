# Example config for managing the existing PIPE-OS project

PROJECT_NAME=pipe-os
TARGET_PROJECT=/Users/hans/Code/PIPE/PIPE-OS

# Persistent data locations (keep these outside the target project)
BRAIN_DATA_PATH=${HOME}/.ceo-harness/pipe-os/brain
CONVERSATIONS_PATH=${HOME}/.ceo-harness/pipe-os/conversations
KANBAN_STATE_PATH=${HOME}/.ceo-harness/pipe-os/kanban

# Optional: Point to your Hermes / GBrain / Overstory instances
# HERMES_URL=http://localhost:8081
# BRAIN_URL=http://localhost:8001
